import XCTest
@testable import SwiftUIViewRepresentable

final class SwiftUIViewRepresentableTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(SwiftUIViewRepresentable().text, "Hello, World!")
    }
}
